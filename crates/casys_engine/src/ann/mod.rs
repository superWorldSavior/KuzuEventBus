//! Approximate Nearest Neighbors (ANN) — placeholders

// Index vectoriel (HNSW, etc.) sera ajouté ici, avec persistance par label/propriété
// et compatibilité branches/snapshots via base+delta et compactions.
