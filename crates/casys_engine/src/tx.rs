//! Transaction facades (placeholders). Real logic stays in `engine` crate for now.

/// Read-only transaction facade (placeholder)
#[derive(Debug, Default)]
pub struct TxRead;

/// Write transaction facade (placeholder)
#[derive(Debug, Default)]
pub struct TxWrite;
