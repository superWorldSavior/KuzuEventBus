// Tenant entity barrel (minimal)
export type { Tenant, TenantId, TenantName } from "./model";
export {
  TENANT_ID_PREFIX,
  TENANT_NAME_REGEX,
  MIN_TENANT_NAME_LENGTH,
  MAX_TENANT_NAME_LENGTH,
} from "./model";
